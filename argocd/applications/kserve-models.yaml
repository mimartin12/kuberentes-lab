apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kserve-models
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - list:
      elements: []
  template:
    metadata:
      name: '{{.model | normalize }}-kserve'
    spec:
      project: default
      source:
        repoURL: https://github.com/mimartin12/kuberentes-lab.git
        targetRevision: HEAD
        path: argocd/applications/manifests/kserve-models/{{.model | normalize }}
      destination:
        server: https://kubernetes.default.svc
        namespace: kserve-test
      syncPolicy:
        automated:
          prune: true
          selfHeal: true